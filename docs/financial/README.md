# Financial Work Index

The source workbooks are immutable. No model-vNext was built in Phase 1. Current planning/model findings and unresolved inputs are recorded in the ratified canon and assumption register. Future model inputs must conform to [`schemas/model-input-contract.schema.json`](../../schemas/model-input-contract.schema.json).
